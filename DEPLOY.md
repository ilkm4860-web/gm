# Görkem Makine Public Deploy\n\nRender: New > Web Service. Build: `npm install` ; Start: `npm start`.\n\nEnvironment: `NODE_ENV=production`, `ADMIN_PASSWORD=<yönetici şifresi>`, `SESSION_SECRET=<uzun rastgele>`, `PUBLIC_BASE_URL=https://alanadiniz.com`, `TOKENS_DIR=/opt/render/project/src/data`.\n\nTokenların restart/deploy sonrası korunması için persistent disk kullan ve mount path olarak `/opt/render/project/src/data` ver.\n